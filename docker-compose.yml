version: '3'

services:
    dbcontext:
        image: mysql:5.7
        volumes:
            - "/home/database:/var/lib/mysql"
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: "@chocon"
            
    kscapi:
        build:
            context: .
            dockerfile: Dockerfile
        depends_on:
            - dbcontext
        ports:
            - "5000:5000"
        restart: always
        
    kscweb:
        build:
            context: src/web
            dockerfile: Dockerfile
        depends_on:
            - kscApi
        ports:
            - "3000:3000"